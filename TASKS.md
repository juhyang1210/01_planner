# Smart Campus Planner 구현 작업 계획 (TASKS)

이 문서는 PRD.md에 정리된 요구사항과 MVP 범위를 바탕으로, 전체 프로그램을 한 번에 만들지 않고
**3단계로 나누어 순서대로** 구현하기 위한 작업 계획입니다.

각 작업은 이전 작업에서 만든 결과물을 그대로 이어받아 사용하도록 순서를 정했습니다.
(1단계 없이는 2단계를 만들 수 없고, 1·2단계 없이는 3단계를 만들 수 없는 구조입니다.)

---

## 작업 1. 할 일을 등록하고 저장하는 기본 기능 만들기

### 한 줄 설명
"할 일을 적어두고, 브라우저를 껐다 켜도 사라지지 않게" 만드는 단계입니다.

### 쉬운 설명 (비전공자용)
가장 먼저, 종이 다이어리에 해당하는 **가장 기본적인 할 일 목록 기능**을 만듭니다.
할 일을 적고, 어떤 종류(BK/랩실/프로젝트/스터디/개인)인지, 얼마나 중요한지(상/중/하),
언제부터 언제까지 해야 하는지를 함께 적을 수 있게 합니다.
다 한 일은 체크 표시를 하고, 필요 없는 항목은 지울 수 있습니다.
그리고 이렇게 적은 내용은 브라우저를 새로고침하거나 다시 열어도 그대로 남아있도록 만듭니다.
이 단계가 끝나면, 앱을 켤 때마다 내가 적어둔 할 일들이 그대로 보이는 "나만의 할 일 목록장"이 완성됩니다.

### 포함되는 PRD 요구사항
- 할 일 추가
- 카테고리 설정 (BK / 랩실(수업) / 프로젝트 / 스터디 / 개인)
- 중요도 설정 (상 / 중 / 하)
- 시작일과 마감일 지정
- 완료 체크 및 삭제
- 새로고침해도 할 일이 유지되도록 저장

### 왜 이 작업들을 묶었나
할 일을 "만들고, 저장하고, 화면에 뿌려주는" 기능은 서로 뗄 수 없이 연결되어 있습니다.
카테고리·중요도·기간도 결국 할 일 하나에 딸린 정보이므로 같이 만드는 것이 자연스럽고,
저장 기능이 없으면 다음 단계인 완료율 계산이나 캘린더 표시도 의미가 없기 때문에
가장 먼저 완성해야 하는 기초 공사에 해당합니다.

### 이 단계가 끝나면 확인할 수 있는 것
- 할 일을 새로 추가하면 목록에 바로 나타난다.
- 할 일마다 카테고리, 중요도, 시작일/마감일이 표시된다.
- 체크 표시로 완료 여부를 바꿀 수 있고, 삭제 버튼으로 목록에서 제거할 수 있다.
- 새로고침(F5)을 해도 목록이 그대로 남아있다.

---

## 작업 2. 완료율 확인 및 목록 화면 다듬기

### 한 줄 설명
"내가 지금까지 얼마나 했는지 한눈에 보여주고, 목록을 더 보기 쉽게" 만드는 단계입니다.

### 쉬운 설명 (비전공자용)
1단계에서 만든 할 일 목록을 바탕으로, 전체 할 일 중에서 몇 퍼센트를 끝냈는지
숫자(퍼센트)나 막대 형태로 화면 상단에 보여줍니다.
할 일을 추가하거나 완료 체크를 하거나 지울 때마다 이 퍼센트가 자동으로 다시 계산됩니다.
또한 카테고리마다 다른 색으로 표시하거나, 중요도가 높은 항목을 눈에 띄게 표시하는 등
목록 자체를 더 보기 편하게 다듬어서, 어떤 할 일이 더 급하고 중요한지 한눈에 파악할 수 있게 합니다.

### 포함되는 PRD 요구사항
- 전체 완료율 표시
- 카테고리별 시각적 구분 (색상 등)
- 중요도에 따른 우선순위 구분 표시

### 왜 이 작업들을 묶었나
완료율 계산과 목록의 시각적 구분은 모두 1단계에서 만든 "할 일 데이터"가 있어야만
가능한 기능입니다. 즉, 새로운 데이터를 만드는 작업이 아니라 1단계에서 쌓인 데이터를
"계산하고 예쁘게 보여주는" 작업이므로 한데 묶었고, 데이터 구조가 먼저 잡혀 있어야
정확하게 계산하고 표시할 수 있기 때문에 1단계 다음 순서로 배치했습니다.

### 이 단계가 끝나면 확인할 수 있는 것
- 화면에 "전체 완료율 OO%" 같은 표시가 나타난다.
- 할 일 상태를 바꾸면(완료 체크, 추가, 삭제) 완료율이 즉시 갱신된다.
- 목록에서 카테고리별로 색이 다르게 보이고, 중요도가 높은 항목이 구분되어 보인다.

---

## 작업 3. 한 달 캘린더로 일정 보기

### 한 줄 설명
"내 할 일들을 달력 위에 펼쳐놓고 한 달 전체 일정을 한눈에" 보는 단계입니다.

### 쉬운 설명 (비전공자용)
마지막으로, 지금까지 등록한 할 일들을 목록이 아니라 **달력 형태**로도 볼 수 있게 만듭니다.
달력의 각 날짜 칸에는 그 날짜에 해당하는 할 일(시작일부터 마감일 사이에 포함되는 일정)이
표시되고, 카테고리별로 색깔이 달라서 한눈에 어떤 종류의 일정인지 알아볼 수 있습니다.
"이전 달", "다음 달" 버튼을 눌러 원하는 달의 일정도 확인할 수 있습니다.
이 단계까지 끝나면, 목록으로도 캘린더로도 내 모든 일정을 확인할 수 있는
Smart Campus Planner MVP가 완성됩니다.

### 포함되는 PRD 요구사항
- 한 달 캘린더로 일정을 한 번에 볼 수 있도록 표시
- 시작일~마감일 기간이 캘린더 표시와 연동
- 카테고리별 색상 구분(캘린더에서도 식별 가능)
- 월 이동(이전 달/다음 달) 기능

### 왜 이 작업들을 묶었나
캘린더 기능은 1단계에서 만든 할 일 데이터(특히 시작일/마감일, 카테고리)를 그대로 가져와
"목록"이 아닌 "달력" 형태로 다시 보여주는 것입니다. 데이터를 만드는 로직과 저장 로직이
이미 안정적으로 갖춰져 있어야 캘린더에 정확하게 연결할 수 있고, 카테고리 색상 구분도
2단계에서 이미 정의한 방식을 그대로 재사용할 수 있으므로 마지막 단계로 배치했습니다.

### 이 단계가 끝나면 확인할 수 있는 것
- 한 달치 달력이 화면에 표시되고, 오늘 날짜를 포함한 각 날짜 칸이 보인다.
- 할 일을 등록한 기간에 해당하는 날짜 칸에 그 할 일이 표시된다.
- 카테고리에 따라 캘린더에서도 색으로 구분된다.
- "이전 달 / 다음 달" 버튼으로 다른 달의 일정도 확인할 수 있다.

---

## 전체 순서 요약

| 단계 | 핵심 목표 | 선행 조건 |
|------|-----------|-----------|
| 작업 1 | 할 일 추가 · 분류 · 완료/삭제 · 저장(새로고침 유지) | 없음 (최초 시작) |
| 작업 2 | 완료율 표시 · 목록 시각적 구분 | 작업 1의 할 일 데이터 구조 |
| 작업 3 | 한 달 캘린더 뷰 · 월 이동 | 작업 1의 날짜/카테고리 데이터, 작업 2의 색상 구분 규칙 |

> 참고: 본 문서는 계획 정리 문서이며, 실제 프로그램 코드는 아직 작성하지 않았습니다.
